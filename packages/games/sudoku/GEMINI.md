# 项目规则

- 所有的交流和文档必须只使用中文。
- 提交 Git 时，由 AI 评估并决定是否更新相关文档（如 README.md, GEMINI.md 等）。
- 更新 `GEMINI.md` 后，必须提交 Git，以确保文档与代码库状态保持一致。
- **Shell 执行规则**：由于当前环境为 PowerShell 5.1，执行多条命令时禁止使用 `&&`。应使用分号 `;` 或分多步执行，以确保兼容性。
